#include <iostream>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    //Kiểm tra n >= 500 và n < 1000
    int n = 800;
    if ((n >= 500) && (n < 1000)) {
        cout << "n nam giua 500 va 1000";
    }
}